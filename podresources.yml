apiVersion: v1
kind: Pod
metadata:
  name: resources
spec:
  containers:
  - name: resources 
    image: nginx
    resources:
      requests: 
        memory: "64Mi"
        cpu: "100m"
      limits:
        memory: "128Mi"
        cpu: "200m"

