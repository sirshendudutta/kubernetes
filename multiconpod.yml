apiVersion: v1
kind: Pod
metadata:
        name: multiconpod
spec:
        containers:
        - name: nginx
          image: nginx
        - name: redis
          image: redis
        - name: memcached
          image: memcached
          
